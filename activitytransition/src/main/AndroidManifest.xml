<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bobmechanic.activitytransition">
    <!-- Required to access activity recognition. -->
    <!-- TODO: Add both activity recognition permissions to the manifest. -->
    <!-- Required for 28 and below. -->
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" /> <!-- Required for 29+. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application>
        <activity android:name=".view.permission.PermissionActivity"></activity>
        <activity android:name=".view.transitionapi.ActivityTransitionApiActivity" />
    </application>

</manifest>